intelligence app, inbox items are worknotes items.

# GETTING THE APP SETUP

# ENV variables for local prod can be fetched from env. variables in vercel console. use the .env template located in this folder to setup your local instance, copy into a .env that you create for EACH root folder, get missing vars from vercel env, keep the pasted template vars the same (urls, etc)

npm i

# run backend
cd backend
npm run dev
# run frontend
cd frontend
npm run dev

# run mcp
cd mcp
npm run start


## FUTURE FEATURES:

## FUTURE SUGGESTIONS: CONVERT BACKEND TO TYPESCRIPT? (not critical, but can help with stability)